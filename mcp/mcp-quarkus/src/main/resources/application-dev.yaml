quarkus:
  devservices:
    enabled: true
  dynamodb:
    endpoint-override: http://localhost:8000
    aws:
      region: eu-central-1
      credentials:
        type: static
        static-provider:
          access-key-id: test
          secret-access-key: test