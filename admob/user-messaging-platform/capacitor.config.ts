import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ionic.starter',
    appName: 'user-messaging-platform',
    webDir: 'www'
};

export default config;
