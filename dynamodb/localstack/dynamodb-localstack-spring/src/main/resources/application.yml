spring:
  docker:
    compose:
      file: classpath:compose.yml
  cloud:
    aws:
      credentials:
        access-key: testKey
        secret-key: testSecret
      dynamodb:
        region: eu-central-1
        endpoint: "http://localhost:4566"